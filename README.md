# NavCaster

A prototype maritime weather forecasting and navigation application.

## Run locally

You will need to define a VITE_MAPBOX_TOKEN env variable in navcaster/apps/ui/.env to view the background map

```bash
npm install
npm run dev
```
